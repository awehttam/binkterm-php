{% extends "base.twig" %}

{% block title %}{{ title }} - {{ system_name }}{% endblock %}

{% block content %}
<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2><i class="fas fa-server"></i> Node Details</h2>
                <a href="/nodelist" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Back to List
                </a>
            </div>

            <div class="row">
                <div class="col-lg-8">
                    <div class="card">
                        <div class="card-header">
                            <h4 class="mb-0">
                                {{ node.full_address }}
                                {% if node.keyword_type %}
                                    <span class="badge bg-primary ms-2">{{ node.keyword_type }}</span>
                                {% endif %}
                            </h4>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-6">
                                    <h6>System Information</h6>
                                    <table class="table table-sm">
                                        <tr>
                                            <th width="120">System Name:</th>
                                            <td>{{ node.system_name|default('Not specified') }}</td>
                                        </tr>
                                        <tr>
                                            <th>Sysop:</th>
                                            <td>{{ node.sysop_name|default('Not specified') }}</td>
                                        </tr>
                                        <tr>
                                            <th>Location:</th>
                                            <td>{{ node.location|default('Not specified') }}</td>
                                        </tr>
                                        <tr>
                                            <th>Phone:</th>
                                            <td>
                                                {% if node.phone and node.phone != '-Unpublished-' %}
                                                    {{ node.phone }}
                                                {% else %}
                                                    <span class="text-muted">Unpublished</span>
                                                {% endif %}
                                                {% if node.flags['INA'] is defined %}
                                                    <br>{{ node.flags['INA'] }}
                                                {% endif %}
                                            </td>
                                        </tr>
                                        <tr>
                                            <th>Baud Rate:</th>
                                            <td>
                                                {% if node.baud_rate > 0 %}
                                                    {{ node.baud_rate }}
                                                {% else %}
                                                    <span class="text-muted">Not specified</span>
                                                {% endif %}
                                            </td>
                                        </tr>
                                    </table>
                                </div>
                                <div class="col-md-6">
                                    <h6>Address Components</h6>
                                    <table class="table table-sm">
                                        <tr>
                                            <th width="80">Zone:</th>
                                            <td>{{ node.zone }}</td>
                                        </tr>
                                        <tr>
                                            <th>Net:</th>
                                            <td>{{ node.net }}</td>
                                        </tr>
                                        <tr>
                                            <th>Node:</th>
                                            <td>{{ node.node }}</td>
                                        </tr>
                                        <tr>
                                            <th>Point:</th>
                                            <td>{{ node.point }}</td>
                                        </tr>
                                        <tr>
                                            <th>Full:</th>
                                            <td><strong>{{ node.full_address }}</strong></td>
                                        </tr>
                                    </table>
                                </div>
                            </div>

                            {% if node.flags and node.flags|length > 0 %}
                                <hr>
                                <h6>Node Capabilities & Flags</h6>
                                <div class="row">
                                    {% for flag_name, flag_value in node.flags %}
                                        <div class="col-md-4 mb-2">
                                            <span class="badge bg-info me-1">{{ flag_name }}</span>
                                            {% if flag_value != true and flag_value != '1' %}
                                                <small class="text-muted">{{ flag_value }}</small>
                                            {% endif %}
                                        </div>
                                    {% endfor %}
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>

                <div class="col-lg-4">
                    <!-- Actions Card -->
                    <div class="card mb-3">
                        <div class="card-header">
                            <h6 class="mb-0">Quick Actions</h6>
                        </div>
                        <div class="card-body">
                            <div class="d-grid gap-2">
                                {% if user %}
                                <a href="/compose/netmail?to={{ node.full_address|url_encode }}&to_name={{ node.sysop_name|url_encode }}" class="btn btn-success">
                                    <i class="fas fa-envelope"></i> Send Netmail
                                </a>
                                {% else %}
                                <a href="/login" class="btn btn-outline-primary">
                                    <i class="fas fa-sign-in-alt"></i> Login to Send Netmail
                                </a>
                                {% endif %}
                                {% if node.flags and node.flags['FREQ'] is defined %}
                                    <button class="btn btn-info" onclick="alert('File request feature coming soon!')">
                                        <i class="fas fa-download"></i> File Request
                                    </button>
                                {% endif %}
                            </div>
                        </div>
                    </div>

                    <!-- Connection Info Card -->
                    {% if node.flags and node.flags|length > 0 %}
                        <div class="card">
                            <div class="card-header">
                                <h6 class="mb-0">Connection Information</h6>
                            </div>
                            <div class="card-body">
                                {% set connection_info = [] %}
                                
                                {% if node.flags['IBN'] is defined %}
                                    {% set connection_info = connection_info|merge(['Internet (BinkP)']) %}
                                {% endif %}
                                
                                {% if node.flags['INA'] is defined %}
                                    {% set connection_info = connection_info|merge(['Internet (Telnet)']) %}
                                {% endif %}
                                
                                {% if node.flags['ITN'] is defined %}
                                    {% set connection_info = connection_info|merge(['Internet (Telnet)']) %}
                                {% endif %}
                                
                                {% if node.phone and node.phone != '-Unpublished-' %}
                                    {% set connection_info = connection_info|merge(['Dialup (POTS)']) %}
                                {% endif %}

                                {% if connection_info|length > 0 %}
                                    <ul class="list-unstyled mb-0">
                                        {% for info in connection_info %}
                                            <li><i class="fas fa-check text-success"></i> {{ info }}</li>
                                        {% endfor %}
                                    </ul>
                                {% else %}
                                    <p class="text-muted mb-0">No connection information available</p>
                                {% endif %}

                                {% if node.flags['CM'] is defined %}
                                    <div class="alert alert-info mt-2 mb-0 py-2">
                                        <small><i class="fas fa-clock"></i> Continuous Mail (24/7)</small>
                                    </div>
                                {% endif %}

                                {% if node.flags['HOLD'] is defined %}
                                    <div class="alert alert-warning mt-2 mb-0 py-2">
                                        <small><i class="fas fa-pause"></i> Mail on Hold</small>
                                    </div>
                                {% endif %}

                                {% if node.flags['DOWN'] is defined %}
                                    <div class="alert alert-danger mt-2 mb-0 py-2">
                                        <small><i class="fas fa-times"></i> System Down</small>
                                    </div>
                                {% endif %}
                            </div>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}